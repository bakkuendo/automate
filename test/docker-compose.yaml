services:
  redis:
    image: redis:latest
    container_name: automate-redis-test
    ports:
      - "6378:6379"
    volumes:
      - redis_data:/data
    command: redis-server --user admin on ">password" ~* "&*" +@all
    restart: unless-stopped

volumes:
  redis_data:
    driver: local
