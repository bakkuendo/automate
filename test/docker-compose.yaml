services:
  cache:
    image: redis:7.4-alpine
    container_name: automate-test
    restart: "no"
    ports:
      - "6378:6379"
    command: redis-server --save 20 1 --loglevel warning --requirepass yourpassword
    volumes:
      - cache:/data

volumes:
  cache:
    driver: local
